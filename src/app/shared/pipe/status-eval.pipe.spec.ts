import { StatusEvalPipe } from './status-eval.pipe';

describe('StatusEvalPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusEvalPipe();
    expect(pipe).toBeTruthy();
  });
});
