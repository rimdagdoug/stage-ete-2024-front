import { ShowButtonPipe } from './show-button.pipe';

describe('ShowButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
